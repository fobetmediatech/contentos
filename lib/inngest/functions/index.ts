/**
 * Registry of all Inngest functions, consumed by the `/api/inngest`
 * route handler. Add new functions to this barrel as you build them.
 */
export { researchNewClient } from "./research-new-client"
